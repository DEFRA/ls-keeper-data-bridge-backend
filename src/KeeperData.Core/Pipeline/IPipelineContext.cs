namespace KeeperData.Core.Pipeline;

// Framework-facing view of the run context. The concrete PipelineContext (domain) implements this,
// so the framework never references domain services directly.
public interface IPipelineContext
{
}
